# TrogonEventStore client tests

The integration suite starts disposable TrogonEventStore nodes using `ghcr.io/trogonstack/trogoneventstore:ci` by default.

Set `TROGON_EVENTSTORE_IMAGE` to test another compatible image.
